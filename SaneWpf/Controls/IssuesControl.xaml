<UserControl x:Class="SaneWpf.Controls.IssuesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaneWpf.Controls"
             xmlns:framework="clr-namespace:SaneWpf.Framework"
             mc:Ignorable="d" 
             x:Name="Control"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
         <ItemsControl ItemsSource="{Binding ElementName=Control, Path=Errors}">
             <ItemsControl.ItemTemplate>
                 <DataTemplate DataType="framework:Validation">
                     <TextBlock Text="{Binding Message}" />
                 </DataTemplate>
             </ItemsControl.ItemTemplate>
         </ItemsControl>

        <ItemsControl ItemsSource="{Binding ElementName=Control, Path=Warnings}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="framework:Validation">
                    <TextBlock Text="{Binding Message}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
